{
  "name": "${packageName}",
  "version": "${integrationVersion}",
  "description": "Trigger.dev integration for ${sdkPackageName}",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist/index.js",
    "dist/index.d.ts",
    "dist/index.js.map"
  ],
  "devDependencies": {${tsconfigDep}${tsupDep}
    "@types/node": "16.x",
    "rimraf": "^3.0.2",
    "tsup": "7.1.x",
    "typescript": "4.9.4"
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && npm run build:tsup",
    "build:tsup": "tsup",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "${latestVersionName}": "^${latestVersion}",
    "${sdkVersionName}": "${sdkVersion}",
    "${integrationKitVersionName}": "${integrationKitVersion}",
    "zod": "3.21.4"
  },
  "engines": {
    "node": ">=16.8.0"
  }
}
