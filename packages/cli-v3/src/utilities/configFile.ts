export type ConfigFileData = {
  accessToken?: string;
};

// export async function readConfigFile(): Promise<ConfigFileData> {
//   const configFile = await fs.readFile(configFilePath, "utf8");
//   return JSON.parse(configFile);
// }
