---
area: webapp
type: feature
---

The AI assistant now gives grounded answers about queues: it names the runs holding concurrency slots, which limit is blocking, and accurate wait times, across all projects in your organization. It also recovers with a clear error instead of hanging on a stuck response, and no longer breaks when code highlighting fails to load.
