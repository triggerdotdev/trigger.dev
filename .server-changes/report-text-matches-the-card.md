---
area: webapp
type: improvement
---

The health report now reads the same in your terminal, in your editor and in the dashboard: same sections, same order, same wording. Where the dashboard uses colour, the text version uses a marker, so nothing is lost when colour isn't available, and a report built on stale data now says so on every surface.
