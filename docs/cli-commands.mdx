---
title: "CLI commands"
description: "The Trigger.dev CLI has a number of commands to help you develop locally and deploy your tasks."
---

import CliDeployCommands from '/snippets/cli-commands-deploy.mdx';
import CliGeneralCommands from '/snippets/cli-commands-general.mdx';

## General commands

<CliGeneralCommands/>

## Deploy commands

Learn more about deploying your tasks in the [Deployment](/deploy-environment-variables) section.

<CliDeployCommands/>


{/* ### Environment

Option                            | Command                         | Description                                                                                  |
:-------------------------------- | :-------------------------------| :------------------------------------------------------------------------------------------- |
Environment                       | `--env` or `-e`                 | Defaults to `prod` but you can specify `staging`.                                            |
Skip typecheck                    | `--skip-typecheck` or `-T`      | Skips the pre-build typecheck step.                                                          |
Build platform                    | `--build-platform`              | The platform to build the deployment image for. Defaults to `linux/amd64`.                   |

| Environment                     |                                                     |
:-------------------------------- | --------------------------------------------------- |
| `--env` or `-e`                 | Defaults to `prod` but you can specify `staging`.   |

| Skip typecheck                  |                                      |
:-------------------------------- | ------------------------------------ |
| `--skip-typecheck` or `-T`      | Skips the pre-build typecheck step.  |

| Build platform                  |                                                                            |
:-------------------------------- | -------------------------------------------------------------------------- |
| `--build-platform`              | The platform to build the deployment image for. Defaults to `linux/amd64`. |

### Environment

```bash
--env
```

Defaults to `prod` but you can specify `staging`. Alternative command: `-e` */}