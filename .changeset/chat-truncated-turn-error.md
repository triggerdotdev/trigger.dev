---
"@trigger.dev/sdk": patch
---

Chat in the browser now shows an error when a reply is cut off by a lost connection that can't be re-established, instead of presenting the partial reply as if it had finished.

Watch-mode viewers now keep receiving later turns instead of the stream closing after the first turn completes.
